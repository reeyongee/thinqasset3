import { FounderLetterExperience } from "@/components/lab/founder-letter/FounderLetterExperience";

export const metadata = {
  title: "Founder Letter — TBG Group | Lab",
  robots: { index: false, follow: false },
};

export default function FounderLetterLabPage() {
  return <FounderLetterExperience />;
}

import { RipeHeroLabContent } from "@/components/lab/ripe-hero/RipeHeroLabContent";

export const metadata = {
  title: "RIPE Hero Lab",
  robots: { index: false, follow: false },
};

export default function RipeHeroLabPage() {
  return <RipeHeroLabContent />;
}
